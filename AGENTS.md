## Agent skills

### Issue tracker

Issues and PRDs live as GitHub Issues at `Sean-Miningah/autotraka-pro`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage state is recorded as a `Status:` line in each issue file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo. Read `CONTEXT.md` at the repo root (when it exists) and `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.