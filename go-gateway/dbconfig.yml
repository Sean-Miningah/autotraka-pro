production:
  dialect: postgres
  datasource: ${DATABASE_URL}
  dir: migrations
  table: migrations

development:
  dialect: postgres
  datasource: ${DATABASE_URL}
  dir: migrations
  table: migrations
